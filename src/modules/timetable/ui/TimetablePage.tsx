import ModulePlaceholder from '../../../shared/components/ModulePlaceholder';
import { Timer } from 'lucide-react';
export default function TimetablePage() { return <ModulePlaceholder title="Timetable" icon={Timer} description="Build and view weekly timetables per stream with period slots." features={['Create weekly timetable', 'Per-stream schedule', 'Subject and teacher allocation', 'View by teacher or stream']} />; }
